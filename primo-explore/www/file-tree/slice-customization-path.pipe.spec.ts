import { SliceCustomizationPathPipe } from './slice-customization-path.pipe';

describe('SliceCustomizationPathPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceCustomizationPathPipe();
    expect(pipe).toBeTruthy();
  });
});
