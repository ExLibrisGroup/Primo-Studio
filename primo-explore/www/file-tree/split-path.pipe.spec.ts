import { SplitPathPipe } from './split-path.pipe';

describe('SplitPathPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitPathPipe();
    expect(pipe).toBeTruthy();
  });
});
