import { OneTimeBindingDirective } from './one-time-binding.directive';

describe('OneTimeBindingDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new OneTimeBindingDirective();
    expect(directive).toBeTruthy();
  });
});
