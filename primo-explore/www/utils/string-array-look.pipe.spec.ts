import { StringArrayLookPipe } from './string-array-look.pipe';

describe('StringArrayLookPipe', () => {
  it('create an instance', () => {
    const pipe = new StringArrayLookPipe();
    expect(pipe).toBeTruthy();
  });
});
